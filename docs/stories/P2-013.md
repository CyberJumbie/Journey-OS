# P2-013: Seed TaskShell Nodes in Neo4j
**Epic:** 2.3 — ECD + TaskShells
**Points:** 2 | **Depends:** P1-006 (Layer 2 seed complete)
**Context packet:** docs/context-packets/CP-EPIC-2.3.md
**Specialist:** @backend-specialist

**As a** system
**I want** 12 TaskShell nodes seeded in Neo4j representing the main NBME question types
**So that** the ECD layer has a concrete structure to attach generated items to

## Acceptance Criteria
- New seeder file: `seeder/src/seed-layer3-taskshells.ts`
- 12 TaskShell nodes, properties: `uuid, shellId, name, bloomMin, bloomMax, vignetteRequired, optionCount, distractorStrategy, createdAt`
- TaskShells to seed:
  1. `TS-001` Clinical Vignette MCQ (Bloom 2–4, vignette required, 5 options, best-worst distractors)
  2. `TS-002` Mechanism of Disease (Bloom 2–3, vignette optional, 5 options)
  3. `TS-003` Diagnosis MCQ (Bloom 3–4, vignette required, 5 options)
  4. `TS-004` Treatment/Management (Bloom 3–5, vignette required, 5 options)
  5. `TS-005` Pathophysiology Chain (Bloom 2–3, vignette optional, 5 options)
  6. `TS-006` Drug Mechanism (Bloom 1–2, no vignette, 5 options)
  7. `TS-007` Lab Interpretation (Bloom 3–4, vignette required, 5 options)
  8. `TS-008` Anatomy/Histology (Bloom 1–2, may include image ref, 5 options)
  9. `TS-009` Prevention/Screening (Bloom 3–4, vignette required, 5 options)
  10. `TS-010` Pharmacotherapy (Bloom 3–5, vignette required, 5 options)
  11. `TS-011` Emergency/Acute Management (Bloom 4–6, vignette required, 5 options)
  12. `TS-012` Epidemiology/Statistics (Bloom 2–4, data table may be present, 5 options)
- Run: `pnpm seed:layer3` in seeder package
- `scripts/validate-graph.ts` updated: check TaskShell count = 12

## Files to Create
- `seeder/src/seed-layer3-taskshells.ts`

## Files to Modify
- `seeder/package.json` — add `seed:layer3` and update `seed:all`
- `scripts/validate-graph.ts` — add TaskShell count assertion

## Smoke Test
```cypher
MATCH (ts:TaskShell) RETURN ts.shellId, ts.name, ts.bloomMin, ts.bloomMax ORDER BY ts.shellId
-- Expected: 12 rows, TS-001 through TS-012
```
